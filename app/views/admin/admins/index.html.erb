<p id="notice"><%= notice %></p>

<h1>Listing Admin Admins</h1>
<%= link_to 'New Admin', new_admin_admin_path %>
<%= datagrid_form_for @grid, :method => :get, :url => @index_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>


